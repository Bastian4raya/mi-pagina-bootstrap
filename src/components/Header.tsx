import React from 'react';

const Header: React.FC = () => {
  return (
    <header className="bg-light p-3">
      <h1 className="text-center">DentiSalud</h1>
    </header>
  );
};

export default Header;
